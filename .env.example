# animbook — 环境配置模板
# 复制为 .env.development / .env.staging / .env.production 后修改具体值
# 命名约定: DEPLOY_ENV 决定当前激活的 .env 文件 (dotenv-webpack 行为)
#
# OpenCode 服务地址: SDK 客户端已硬编码为相对路径 `/ai`.
#   - dev: webpack-dev-server 代理 `/ai/*` 到 127.0.0.1:4096
#   - prod: 由部署方自行配置反向代理 (`/ai` -> opencode 实例)

# 当前环境标识 (development | staging | production)
DEPLOY_ENV=development
