{
  "name": "assistant",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "webapp assistant — opencode AI 服务(子进程),为 web 端提供 AI 推理 / 文件系统 / PTY 终端",
  "scripts": {
    "dev": "opencode serve --port 24096 --hostname 0.0.0.0 --cors \"*\"",
    "start": "opencode serve --port 24096 --hostname 0.0.0.0 --cors \"*\""
  }
}
